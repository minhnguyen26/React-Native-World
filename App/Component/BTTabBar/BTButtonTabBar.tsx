import React from "react";
import { View, TouchableOpacity, Image, SafeAreaView } from "react-native";
import { BTText } from "../BTText";

function BBBottomTabBar({ state, navigation }) {

  return (
    <SafeAreaView>
      <View>
        
      </View>
    </SafeAreaView>

  )
}